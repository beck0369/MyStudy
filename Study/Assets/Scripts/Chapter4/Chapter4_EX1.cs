using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Chapter4_EX1 : MonoBehaviour
{
    // 1. i++와 ++i의 차이점은 무엇인가요?
    //    답 : i++은 i를 출력 후 값을 더해주는 것이고 ++i는 값을 더해준 후 출력하는 것이다

    // 2. 각 출력문의 값을 적으세요
    //    int i = 1;
    //    Debug.Log(i = i + 1); 답 : i = 2
    //    Debug.Log(i++);       답 : i = 2
    //    Debug.Log(++i);       답 : i = 4
    //    Debug.Log(i += 1);    답 : i = 5

    // 3. 다음 코드에서 b는 어떤 값을 가질까요?
    //    int a =10;
    //    string b = a == 0 ? "가나다" : "ABC";
    //    답 : ABC
}
